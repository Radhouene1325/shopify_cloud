
mutation UpdateProductDescription($product: ProductUpdateInput!) {
  productUpdate(product: $product) {
    product {
      id
      title
      descriptionHtml
      metafields(first: 5) {
        nodes {
          namespace
          key
          value
        }
      }
    }
    userErrors {
      field
      message
    }
  }
}